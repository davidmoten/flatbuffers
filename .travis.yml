language: java
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7
  - openjdk6
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
      - gcc-5
      - libc6

before_install:
  - export CC=/usr/bin/gcc-5;
  - export CXX=/usr/bin/g++-5;
